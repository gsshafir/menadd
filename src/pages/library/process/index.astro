---
import { getCollection } from "astro:content";
import Section from "../../../layouts/Section.astro";
import { sections } from "../../../data";

const section = sections.find((item) => item.key === "process")!;
const articles = (await getCollection("articles", ({ data }) => data.section === "process" && !data.draft)).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
---

<Section sectionKey="process" title={section.title} description={section.description} articles={articles} />
